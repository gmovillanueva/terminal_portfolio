import { Terminal } from '@components/Terminal.tsx';
import Layout from '@components/Layout.tsx';

function App() {
  return (
    <Layout>
      <Terminal />
    </Layout>
  );
}

export default App;
